"""Violating shipped-script payload for Git-derived project key detection."""

import subprocess

project_key = subprocess.run(
    ["git", "rev-parse", "--show-toplevel"], capture_output=True, text=True
).stdout.strip()
