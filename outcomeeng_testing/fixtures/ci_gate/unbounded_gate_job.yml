name: Quality Gate

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  check:
    runs-on: ubuntu-latest
    continue-on-error: false
    steps:
      - name: Run quality gate
        run: just check-full
