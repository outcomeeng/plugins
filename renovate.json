{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests",
    "github>outcomeeng/gh-actions//renovate-presets/gh-actions-consumer",
    ":semanticCommitTypeAll(ci)",
    ":dependencyDashboard"
  ],
  "timezone": "UTC",
  "schedule": ["before 6am on monday"],
  "labels": ["dependencies"],
  "prHourlyLimit": 4,
  "prConcurrentLimit": 10,
  "packageRules": [
    {
      "description": "Group GitHub Actions and reusable-workflow updates. Digest pinning itself is enforced by the helpers:pinGitHubActionDigests preset above; this rule only adds the grouping.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    },
    {
      "description": "Group outcomeeng/gh-actions reusable workflow updates from the shared consumer preset.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["outcomeeng/gh-actions"],
      "groupName": "outcomeeng/gh-actions reusable workflows"
    }
  ],
  "customManagers": [
    {
      "description": "Track the CLI tool versions pinned as env vars in the eval and quality-gate workflows. Each pinned line carries an inline `# renovate: datasource=... depName=...` annotation naming its source.",
      "customType": "regex",
      "managerFilePatterns": [
        "^\\.github/workflows/spec-tree-evals\\.yml$",
        "^\\.github/workflows/check\\.yml$"
      ],
      "matchStrings": [
        "\"(?<currentValue>[^\"]+)\" # renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)"
      ]
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
