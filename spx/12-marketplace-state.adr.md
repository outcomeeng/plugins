# Marketplace State Ownership

Marketplace installation separates plugin availability, activation, and refresh: Codex keeps available plugins in the selected `CODEX_HOME`, defaults their home activation to disabled, and lets trusted products enable their chosen skills through `.codex/config.toml`. Persistent refresh preserves each agent's selected plugin set and activation through native update operations, stopping before mutation on a source mismatch; isolated verification exercises installation in disposable homes. Marketplace-delivered Codex subagent definitions remain globally registered in the selected home, independently of product skill activation.

Persistent selection is bounded and ordered by each agent's committed marketplace catalog. Claude Code supplies its installed `outcomeeng` inventory at project scope in the invocation checkout. Codex selection comes from the plugin keys declared in the selected home's configuration, including disabled entries and entries whose caches are absent; merged product overrides and cache presence do not choose that home-wide set. Every nonempty selection contains `spec-tree`. An empty selection supplies only `spec-tree` and a warning to choose additional plugins; an empty Codex home receives it disabled. Claude Code bootstrap preserves any declared project activation and defaults an undeclared bootstrap plugin to disabled.

The canonical persistent marketplace source is the GitHub repository `outcomeeng/plugins`. An existing registration with a different source, or a colliding Claude Code user-scope registration, stops the complete run before mutation and requires explicit repair. A missing registration may be added for bootstrap or recovery of declared selection. Refresh updates every selected plugin, including disabled plugins, and repairs missing snapshots or caches without widening selection. Existing activation entries remain unchanged, including explicitly enabled home entries. Verification reports an unexpected selection or activation change as failure and preserves the diagnostic state; refresh never restores an old settings snapshot. A selected plugin absent from the canonical marketplace is pending publication only when that absence is established; every other operational failure stops the run. Isolated verification registers the invocation checkout, where an absent declared plugin is terminal.

For an agent whose plugin manifest cannot declare agents, the build emits each plugin's agent definitions in that agent's native format inside the plugin's declared skill surface, and plugin installation carries them to the consumer. Every agent definition invokes its plugin's skills, so placement is derived, never chosen: a plugin's agent definitions live in the same scope as the skill content they invoke, and every marketplace-shipped operation that delivers or refreshes either carries both in the same run. Skills installed in the selected agent home place their agents in that home's agent directory; skill content a repository commits carries its agents committed beside it in that checkout. Placing a plugin's agent definitions into a checkout's agent directory is checkout materialization, and it is valid only where that checkout carries the plugin's invoked skill content. A split — agent definitions in one scope while the skills they invoke live in another — is a detected fault: placement reports the mismatched definitions and stops rather than refreshing around them, because a checkout definition shadows the home copy while the home skills underneath it move with every refresh. No installation run or generated instruction surface requires committing agent definitions into a consumer repository, and a checkout's agent directory changes only within the namespace the owning plugin controls.

## Rationale

Plugin availability lets several products share one installed cache while choosing their own skill context. Disabled home defaults make product activation explicit; preserving an existing enabled home entry respects the operator's choice. `spec-tree` is the minimum coherent selection because it supplies the marketplace's governing methodology. Selection declarations survive an interrupted installation or a missing cache, so Codex refresh reads them independently of installed-file discovery. Native update operations preserve activation without a compensating write that could erase failure evidence.

A persistent run installs from the canonical marketplace while bounding the installed inventory with the checkout catalog, so the two disagree precisely when a selected checkout catalog name is absent from the published source. Reporting that name as pending publication keeps the refresh accurate about what it installed, leaves the rest of the selected set installable, and preserves release handling for a checkout whose catalog leads its canonical source. The distinction is the marketplace source, not the plugin: an isolated run registers the checkout itself, so the same absence there means the committed catalog and the built tree disagree, which is a defect and stays terminal.

A persistent install is an explicit request to refresh the selected project and agent home. Claude Code project scope preserves the developer's user scope as a separate ownership boundary. Refusing a same-name user registration prevents flattened marketplace resolution from selecting the wrong source. Trusted Codex product configuration controls effective skill activation, while home configuration controls home-wide refresh selection. Native commands run with that same separation so merged product overrides cannot change the planned set. Automatic source removal and re-addition can destroy installed state; a source mismatch therefore requires an explicit repair outside refresh. Isolated mode protects persistent installation state during verification.

Codex discovers home subagent definitions independently of plugin activation. A global registry keeps placement and ownership reconciliation bounded, while each product controls its skill context. The [native plugin manifest](https://github.com/openai/codex/blob/main/codex-rs/core-plugins/src/manifest.rs) supplies no agent-definition delivery field, so the plugin lifecycle's shipped placement script remains the bridge from packaged definitions to the home registry. Product activation neither generates a checkout registry nor prunes home definitions.

The [Codex configuration contract](https://learn.chatgpt.com/docs/config-file/config-reference) permits trusted-product overrides of local-marketplace plugin activation and refresh of configured disabled plugins. That separation supports a shared home cache with product-selected skills; preserving activation through recovery and excluding product overrides during refresh remain obligations of the marketplace installer.

Agent definitions are placed beside their skills because an agent whose plugin manifest declares only skills has no manifest path to its agents: that agent loads custom subagent definitions from its agent directories alone — the selected agent home and the checkout — so a plugin's agents reach a session only once something writes them into one of those. An agent definition is a pointer into its plugin's skill content, so the two are one versioned unit: the placement scope follows the skills, and every marketplace-shipped operation that refreshes the skills refreshes the agents in the same pass. Installation installs skills into the selected agent home, so home placement is what installation performs; a committed checkout copy is coherent only where the checkout also carries the skill content it invokes. The split placement is the failure the co-location rule exists to prevent — a checkout definition shadows the home copy while the home skills underneath it move with every refresh, and committing one machine's plugin state into repositories that do not own it multiplies that drift — so placement detects the split and stops with a report instead of writing around it. Conversion belongs to the build because it is a pure function of the agent source: running it once emits each agent's native artifact, so every consumer receives byte-identical definitions rather than whatever each machine's converter produced, and the placement a consumer performs stays a file copy rather than a second implementation of the mapping. Shipping those artifacts inside the plugin's own declared skill surface makes delivery contractual — a declared component the installer is obliged to carry — rather than depending on an undeclared directory surviving the copy. The rejected alternative defers conversion to each consumer, which multiplies a mapping implementation across machines and leaves the agent surface reachable only where that conversion runs.

## Invariants

- Every installation plan is bounded and ordered by the corresponding committed marketplace catalog.
- A persistent plan refreshes only the agent's pre-run selected catalog plugins, except that empty state selects only `spec-tree` and carries a warning.
- A nonempty persistent selection contains `spec-tree` or the run stops before its first state-changing command.
- Persistent Claude Code installation resolves only at project scope after proving no user-scope `outcomeeng` registration exists.
- Persistent Codex installation resolves only inside the selected `CODEX_HOME`.
- Codex home selection includes configured disabled plugins and survives missing installation caches; trusted product activation cannot widen or narrow that selection.
- Bootstrap defaults to disabled without changing an existing activation declaration; refresh preserves every pre-run activation entry.
- A registered source mismatch rejects the complete plan before mutation; automatic source replacement is outside refresh.
- Isolated verification resolves installation and session state beneath caller-selected disposable homes. Subscription discovery alone may read and natively refresh the selected saved-login file, as governed by `spx/32-distribution.enabler/21-installation.enabler/21-repository-installation.enabler/21-installation-architecture.adr.md`.
- Successful persistent installation leaves pre-run plugin selection and activation unchanged, apart from the declared empty-selection bootstrap.
- Persistent installation reports established canonical-source absence as pending publication and continues with the remaining selected plugins until completion or the first other failure; isolated verification treats that absence as terminal.
- A plugin's agent definitions live in the same scope as the skill content they invoke, and every marketplace-shipped operation that delivers or refreshes either carries both in the same run; installation-delivered skills in the selected agent home carry their agent definitions into that home's agent directory.
- Placement never writes around a scope split: agent definitions found in a scope whose skill content lives elsewhere are reported as a fault and left for directed repair, never silently refreshed, pruned, or duplicated.
- Agent-home cleanup beyond one plugin's namespace is a marketplace-scope reconciliation, a distinct operation from any plugin's namespace-bounded placement; it prunes or overwrites only definitions under the marketplace's recorded ownership, and a colliding file outside that ownership is reported and left untouched.

## Verification

- ALWAYS: persistent selection uses Claude Code's project-installed inventory and Codex's selected-home plugin declarations, including disabled entries, bounded and ordered by the corresponding committed catalog.
- ALWAYS: trusted product `.codex/config.toml` may enable chosen skills over disabled home defaults, while persistent Codex inspection and execution remain independent of product overrides.
- ALWAYS: empty persistent selection supplies only `spec-tree` with a warning, disabled where no activation is declared; an empty Codex home declares `spec-tree` disabled.
- ALWAYS: refresh uses native operations that preserve the selected set and activation, including disabled plugins, and verifies both after execution; a mismatch is reported without restoring an older configuration snapshot.
- NEVER: refresh resets an existing explicitly enabled home entry to the disabled default.
- NEVER: an existing noncanonical marketplace source reaches a state-changing operation; the diagnostic identifies the source and requires explicit repair.
- ALWAYS: recovery of a missing registration, snapshot, or selected plugin cache preserves the declared selection and activation, including a retry after interrupted installation.
- ALWAYS: Codex subagent definitions for home-selected plugins remain in the global selected-home registry independently of trusted-product skill activation; plugin lifecycle placement retains its namespace and digest-bound ownership checks.
- NEVER: product skill activation generates a checkout subagent registry or filters the home registry.

### Testing

- ALWAYS: for an agent whose plugin manifest cannot declare agents, that plugin's agent definitions ship inside the plugin's own declared skill surface and persistent installation places them in the selected agent home's agent directory — no manifest path delivers them ([compliance])
- ALWAYS: a plugin's lifecycle skill places and prunes only within the namespace its own plugin owns, leaving agent definitions authored by the developer or provided by another plugin untouched ([compliance])
- ALWAYS: agent-home cleanup beyond one plugin's namespace runs as the marketplace-scope reconciliation, distinct from any plugin's namespace-bounded placement, and establishes marketplace-scoped ownership — an ownership record or content marker, never a filename prefix alone — before any prune or overwrite in the shared agent home, reporting a colliding foreign file and leaving it untouched ([compliance])
- ALWAYS: placement derives each plugin's destination scope from where that plugin's skill content lives, and a scope split — agent definitions present in a scope whose invoked skill content lives elsewhere — is reported as a fault with no refresh, prune, or duplicate written across it ([compliance])
- NEVER: a plugin's agent definitions ship through a directory the plugin manifest does not declare — an undeclared surface reaching the consumer is incidental, not contractual ([compliance])

- NEVER: a nonempty persistent agent selection omitting `spec-tree` reaches a state-changing command ([compliance])
- ALWAYS: persistent installation reports established canonical-source absence as pending publication and continues with the remaining selected plugins until completion or the first other failure, while isolated verification treats the same absence as terminal at that plugin's operation ([compliance])
- NEVER: persistent installation mutates Claude Code user scope; a user-scoped `outcomeeng` marketplace registration stops the run before its first state-changing command ([compliance])
- ALWAYS: isolated verification confines `HOME`, `CLAUDE_CONFIG_DIR`, `CODEX_HOME`, and `CODEX_SQLITE_HOME` beneath caller-selected disposable state; subscription discovery may read and natively refresh only the selected saved-login file, leaving persistent installation state unchanged ([compliance])
- ALWAYS: installation derives marketplace membership from committed per-agent catalogs — Codex in `.agents/plugins/marketplace.json` and Claude Code in `.claude-plugin/marketplace.json` ([mapping])
- ALWAYS: marketplace-install diagnosis derives expected plugin state from the checkout's committed per-agent marketplace catalogs rather than from a plugin catalog embedded in shipped diagnostic output ([compliance])
- ALWAYS: isolated installation verifies every catalog plugin, a generated valid subset containing `spec-tree`, and a generated invalid subset omitting `spec-tree` in disposable homes without mutating persistent state ([compliance])

### Audit

- ALWAYS: Python marketplace orchestration that invokes agent or lifecycle commands accepts an injected runner implementing a Protocol, while the CLI composition edge binds the real subprocess adapter ([audit])
- ALWAYS: a checkout's agent directory receives a plugin's agent definitions only where that checkout also carries the skill content those definitions invoke — never as a required step of installation or of any generated instruction surface ([audit])
- NEVER: framework mocks or process-global replacement substitute for an agent CLI or lifecycle-command boundary ([audit])
- NEVER: a generated instruction surface directs an agent session to commit agent definitions into a checkout — a missing subagent definition is repaired by refreshing the selected agent home and then reloading the harness plugin index or starting a new session, because a running session retains already-loaded plugin content ([audit])
