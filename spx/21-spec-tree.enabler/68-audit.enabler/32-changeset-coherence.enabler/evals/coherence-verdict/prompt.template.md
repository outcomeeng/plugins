<!-- Generated from the complete producer at {producer_path}. -->

Apply the complete producer's semantic classification workflow to the supplied evidence packet as an already-collected changeset subject. Do not inspect a repository or persist an SPX verification run for this eval case. Return only the producer's structured coherence-result JSON object.

{producer_file}
The changeset evidence packet (JSON-encoded):

```json
{input_json}
```
