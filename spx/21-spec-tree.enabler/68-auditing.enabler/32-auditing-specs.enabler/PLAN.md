# Plan: Spec Auditing

The `audit-specs` skill and `spec-auditor` agent ship; the node is EXCLUDE'd until its eval suites land. Two follow-ups remain.

## Eval suites (un-EXCLUDE the node)

All four `[eval]` suites are authored (`evals/structure/`, `evals/voice/`, `evals/tag-validity/`, `evals/prose-coupling/` — each `eval.toml` + `cases.jsonl` + `prompt.md`), mirroring `spx/21-spec-tree.enabler/32-decisions.enabler/21-adr-auditing.enabler/evals/`. Each case replays a malformed/clean node spec through the `audit-specs` methodology (self-contained prompt, three rows `section-structure` / `atemporal-voice` / `tag-fitness`) and grades the structured verdict. The `tag-validity` suite includes a case proving a bare `([review])` passes (the BLOCKING fix from PR #274); the `prose-coupling` suite proves a `[test]` tag on a skill/spec-prose claim is rejected and the same claim tagged `[audit]` passes. **Logic is verified** — every case produces the right verdict at adequate budget.

Remaining: the suites fail at the **$0.50 CI budget** (`spec-tree-evals.yml`) because loading `plugin_dir = "dist/claude/spec-tree"` costs ~$0.55 in cache creation on the first case, and with 3-4 cases per suite the 0.85 threshold cannot absorb that one budget failure (the 10-case transport-selection suite could). The prompt already forbids tool use (single-turn). Resolve by EITHER pointing each `eval.toml` `plugin_dir` at a minimal plugin (e.g. `dist/claude/frontend`, 6 KB — the self-contained prompt does not use the plugin; **tested: every case then passes at $0.50**) with a `# minimal plugin_dir: the prompt is self-contained; see PLAN` comment, OR expanding each suite to ~8 cases so the threshold absorbs the one cache-creation failure (the conventional transport-selection pattern). Then run all four to `verdict=PASS`, remove the `spx/EXCLUDE` entry, dispatch `spec-auditor` on `auditing-specs.md` (now installed), and `/merge`.

## Tree-wide prose-coupling cleanup

`audit-specs` (mis-tagged `[test]` assertion) and the `audit-tests` prose-coupling rule (prose-grep test) catch markdown-coupling violations laundered through test infrastructure. Once the skills are installed, dispatch `spec-auditor` and `test-evidence-auditor` across the tree and fix the known sites — retag the prose-bound `[test]` assertions to `[eval]`/`[audit]` and replace or remove the prose-grep tests in: `76-merging.enabler` (`test_merge_gate_policy.conformance`, `test_merge.conformance`), `76-merging.enabler/32-github-pr.enabler` (`test_github_pr.conformance`), `13-infrastructure.enabler/21-github-actions.enabler/54-runtime-operations.enabler` (`test_runtime_operations` scenario + property), `68-reviewing.enabler/21-reviewing-changes.enabler` (`test_reviewing_changes`, laundered via the `reviewing_changes` harness), and `76-sessions.enabler/32-skill-surface.enabler` (`test_skill_surface`, laundered via the `skill_surface` harness). Audit each fix and ship per node.
