# Plan: restore sessions test-evidence ownership

Deliver one observable correction: the sessions and pickup evidence proves the declared CLI
behavior through assertion-only test files, source-owned contracts, and resource-managing test
infrastructure, then reaches the default branch through the repository merge lifecycle.

- [ ] Merge the Python test-seam standards dependency to `origin/main`, then sync this branch.
- [ ] Use `/slice`, `/apply`, `/test`, and the Python test/code workflows for the sessions node.
- [ ] Keep comparisons, exception assertions, and pass/fail decisions in the linked test files.
- [ ] Keep protocol values, finite domains, constructors, and policy vocabulary in source contracts.
- [ ] Keep harnesses limited to setup, teardown, execution, and structured observations.
- [ ] Remove unsupported architecture additions and reconcile `ISSUES.md` after the defect is fixed.
- [ ] Regenerate authored plugin output and pass focused deterministic verification.
- [ ] Commit an exact gate subject and converge decision, test-evidence, implementation, and
      whole-changeset verification.
- [ ] Run `just check-full`, merge through `/merge`, refresh the marketplace, and close the
      claimed session through `/handoff`.
