title = "worktree-pool-check"
cases = "cases.jsonl"
prompt = "prompt.md"
plugin_dir = "dist/claude/spec-tree"
owned_paths = [
  "spx/21-spec-tree.enabler/79-diagnostics.enabler/**",
  "src/plugins/spec-tree/skills/diagnose/**",
  "dist/claude/spec-tree/skills/diagnose/**",
]
smoke_cases = ["compliant-bare-pool", "stale-claims-in-pool", "non-compliant-linked-on-non-bare"]
threshold = 0.8
trials = 1
ci_policy = "manual" # excluded from automatic CI grading
