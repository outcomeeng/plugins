# Claude Invocation

PROVIDES the Claude Code runtime invocation substrate — headless CLI invocation, session-trace format, plugin loading, and cache-relevant invocation shape
SO THAT the agent-adapter contract
CAN bind Claude Code as one selectable runtime without consumers knowing runtime-specific session or invocation detail
