# Codex Invocation

PROVIDES the Codex runtime invocation substrate — headless CLI invocation, session-trace format, plugin loading, and bounded execution shape
SO THAT the agent-adapter contract
CAN bind Codex as one selectable runtime without consumers knowing runtime-specific session or invocation detail
