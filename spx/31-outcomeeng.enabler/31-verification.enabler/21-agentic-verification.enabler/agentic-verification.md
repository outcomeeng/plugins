# Agentic Verification

PROVIDES the agent-adapter contract for driving a real coding-agent runtime — a selected skill, session-state input, a bounded invocation, and a structured machine-verifiable result envelope with cost and cache telemetry — through one adapter per runtime
SO THAT evaluate, audit, and review verification surfaces
CAN invoke the runtime behavior they verify through one shared contract instead of simulating a runtime per surface
