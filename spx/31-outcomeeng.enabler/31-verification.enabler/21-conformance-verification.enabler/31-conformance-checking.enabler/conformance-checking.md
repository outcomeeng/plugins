# Conformance Checking

PROVIDES deterministic conformance verdicts — a pure evaluation of one trace against one declared contract, with per-violation evidence locating each finding in the trace
SO THAT verification surfaces and CI gates
CAN judge workflow conformance reproducibly, with no model in the verdict path
