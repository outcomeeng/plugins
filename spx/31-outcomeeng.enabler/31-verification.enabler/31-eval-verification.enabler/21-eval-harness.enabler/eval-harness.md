# Eval Harness

PROVIDES the generic eval harness — suite, case, trial, and verdict vocabulary, deterministic grading, run records, and history — parameterized by skill, session trace, trial count, and expected machine-verifiable outcome, delegating runtime invocation to agent adapters
SO THAT eval suites across plugins
CAN score real runtime behavior against curated cases with one harness regardless of which coding agent produces the behavior
