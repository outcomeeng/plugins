# Distribution

PROVIDES marketplace distribution across downstream skill repositories, plugin release metadata, origin publication, and agent-harness installation
SO THAT marketplace maintainers and plugin consumers
CAN receive consistent built plugin artifacts through every supported delivery path

## Assertions

### Compliance

- NEVER: downstream single-plugin repository distribution includes agent files — those repositories receive skill directories only ([audit])
