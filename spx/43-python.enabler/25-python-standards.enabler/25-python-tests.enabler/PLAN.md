# PLAN — Python test overlay routing

## Pending work

1. Decide whether `spx/local/python-tests.md` loading belongs in `python-test-standards`, `test-python`, or both.
2. Add the overlay read path so product-local Python testing rules affect the test-writing workflow while preserving existing `audit-python-tests` overlay handling.
3. Gate the change with the repository's skill checks and a Python test-evidence audit.

## Revisit condition

Resume when Python test-overlay routing is scheduled for implementation.
