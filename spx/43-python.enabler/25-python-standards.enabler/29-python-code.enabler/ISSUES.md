# Python code workflow issues

## Concrete consumer example

The skill audit recommends one worked example in `code-python` that resolves
the portable placeholders for a representative consumer repository, including
the node test path, production source root, and canonical verification command.

Revisit when the example can remain consumer-neutral and concise without
embedding this marketplace repository's paths or command wrappers.

## Portable command capability and closeout evidence

The skill audit approves the current unrestricted frontmatter but recommends a
future capability contract that preapproves the workflow's file, skill, edit,
and validation operations without hard-coding one consumer's wrappers or
granting broad shell access. It also identifies partly judgment-based success
criteria for standards conformance.

Revisit when the runtime can express portable repository-canonical command
delegation, and bind each closeout criterion to an observable command result or
structured audit result.
